global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'spring-back'
    metrics_path: '/actuator/prometheus'
    basic_auth:
      username: actuator
      password_file: /etc/prometheus/secrets/actuator_password.txt
    static_configs:
      - targets: ['host.docker.internal:8080']
